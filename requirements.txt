# video-stack — deps Python FIJADAS (ninguno de los dos repos upstream fija el lado
# Python; este repo sí — handoff §9, riesgo de mantenimiento).
# Verificadas funcionando juntas el 2026-08-03 (Python 3.10, Windows/Git Bash).
#
# Instalación (venv en la raíz del repo):
#   python -m venv venv
#   venv/Scripts/pip install -r requirements.txt      # Windows
#   ./venv/bin/pip install -r requirements.txt        # macOS/Linux
# En GPU NVIDIA instala antes torch con CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# También en PATH (no pip): ffmpeg + ffprobe, Node.js 18+, y en Windows Git Bash + jq.

# --- núcleo (transcripción, normalizadores, schema)
requests==2.32.5
jsonschema==4.26.0
faster-whisper==1.2.1
ctranslate2==4.8.1

# --- rama shorts (detección de contenido / reframe)
mediapipe==0.10.35
numpy==2.2.6
opencv-python==5.0.0.93

# --- rama longform (thumbnails de /packaging, YouTube upload/stats)
Pillow==12.2.0
google-genai==1.5.0
google-api-python-client==2.190.0
google-auth==2.41.1
google-auth-oauthlib==1.2.2
google-auth-httplib2==0.2.1

# --- chat embebido del cut-editor (tools/editor) — opcional: sin él, el editor
#     funciona igual y el panel de chat se muestra deshabilitado
claude-agent-sdk==0.2.133

# --- rama generador (pipeline/ + server/, ex video-pipeline; versiones del venv
#     que validó las corridas E2E del 2026-08-22/23)
openai==3.3.1
fal-client==1.0.1
langfuse==4.14.4
pydantic==2.13.4
httpx==0.28.1
python-dotenv==1.2.3
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
mangum==0.19.0          # adaptador API Gateway→ASGI (Fase 5 C1; inocuo en local)
PyJWT==2.10.1           # M2: verificación RS256 del id_token de Cognito
cryptography==44.0.1    # backend RSA de PyJWT (se fija: regla de deps del repo)
boto3==1.43.86          # Data API de Aurora (C2) — las imágenes de contenedor
                        # Lambda NO traen boto3 incluido, a diferencia de los
                        # runtimes administrados

# --- tests
pytest==9.1.1
pytest-asyncio==1.4.0
